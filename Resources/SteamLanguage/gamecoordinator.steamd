

class MsgGCHdrProtoBuf
{
	protomaskgc uint msg = 0;
	int headerLength;

	proto<headerLength> SteamKitten.GC.Internal.CMsgProtoBufHeader proto;
};

class MsgGCHdr
{
	ushort headerVersion = 1;

	ulong targetJobID = ulong.MaxValue;
	ulong sourceJobID = ulong.MaxValue;
};
